# Frontend — NFTCollection demo

Demo Next.js 15 (App Router) + ethers v6 + Zod + Vitest.

Ver playbook completo: [`../doc/FRONTEND.md`](../doc/FRONTEND.md).

```bash
cp .env.example .env.local
npm install
npm run dev
npm test
```
