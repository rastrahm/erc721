/**
 * Página 404 de la demo.
 * @returns {JSX.Element}
 */
export default function NotFoundPage() {
  return (
    <section className="panel">
      <h2 className="panel-title">No encontrado</h2>
      <p className="muted">La ruta no existe. Vuelve al inicio.</p>
    </section>
  );
}
